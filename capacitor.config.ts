import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.accenture.todolist',
  appName: 'Accenture-ToDo-List',
  webDir: 'www'
};

export default config;
